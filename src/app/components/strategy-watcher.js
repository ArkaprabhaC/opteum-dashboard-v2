import React, { Fragment } from 'react'
import './strategy-watcher.css'

const Watcher = () => {
    return (
        <main className="strategy_tracker" style={{backgroundColor: "red"}}>
            <p>Live Strategy Tracker</p>
        </main>
    );
}

export default Watcher;